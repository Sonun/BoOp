<Window x:Class="BoOp.UI.WPF.Views.LoginView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:BoOp.UI.WPF.Views"
      mc:Ignorable="d"
      d:DesignHeight="380" d:DesignWidth="280"
      xmlns:viewmodels="clr-namespace:BoOp.UI.WPF.ViewModels"
      d:DataContext="{d:DesignInstance viewmodels:LoginViewModel}"
      Background="#82D5EF">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" VerticalAlignment="Bottom">
            <StackPanel Orientation="Vertical" Grid.Row="0">
                <Label Content="Username:" FontSize="16" />
                <TextBox Text="Test" Style="{StaticResource SearchIconTextBoxTemplate}" />
                <Label Content="Password:" FontSize="16" />
                <TextBox Text="Test" Style="{StaticResource SearchIconTextBoxTemplate}" />
            </StackPanel>
        </Grid>
        <Button Grid.Row="1" Style="{StaticResource RoundedButtonStyle}" Content="Login" />
    </Grid>
</Window>