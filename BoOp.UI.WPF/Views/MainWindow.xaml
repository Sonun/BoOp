<Window x:Class="BoOp.UI.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:viewmodels="clr-namespace:BoOp.UI.WPF.ViewModels"
        xmlns:local="clr-namespace:BoOp.UI.WPF"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <viewmodels:MainWindowViewModel/>
    </Window.DataContext>
    <Grid>
        <TextBox Width="100" Height="100" HorizontalAlignment="Right" Text="{Binding Bookname}"/>
        <TextBox Width="100" Height="100" HorizontalAlignment="Left" Text="{Binding Username}"/>
        <TextBlock Height="100" HorizontalAlignment="Center" Text="{Binding UserOwnsBook}"/>
        <Button VerticalAlignment="Bottom" Content="Clear" Command="{Binding ClearCommand}"/>
    </Grid>
</Window>
