<UserControl x:Class="BoOp.UI.WPF.Views.AdminView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BoOp.UI.WPF.Views"
             mc:Ignorable="d"
             xmlns:viewmodels="clr-namespace:BoOp.UI.WPF.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewmodels:AdminViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height=" 10*"/>
        </Grid.RowDefinitions>


        <Button Grid.Row="0" Grid.Column="1" Content="Zurück" Command="{Binding BackCommand}" Margin="10"/>

        <Grid Grid.Row="0" Grid.Column="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Label Grid.Column="0" Content="Books" Foreground="HotPink" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <Label Grid.Column="1" Content="User" Foreground="HotPink" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Grid>

        <Grid Grid.Column="0" Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <!-- grid for book titles -->
            <Grid x:Name="booklistgrid" Margin="10, 2" IsSharedSizeScope="True">
                <Grid.ColumnDefinitions >
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="2*"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Command="{Binding SortTitleCommand}" >
                    <TextBlock Text="Titel">
                        <Image Source="{StaticResource SortIcon}" Width="16" Height="16" Margin="0, 0, 5, 0"/>
                    </TextBlock>
                </Button>
                <Button Grid.Column="1" Command="{Binding SortAuthorCommand}" >
                    <TextBlock Text="Author">
                        <Image Source="{StaticResource SortIcon}" Width="16" Height="16" Margin="0, 0, 5, 0"/>
                    </TextBlock>
                </Button>
                <Button Grid.Column="2" Content="Exemplare (verfügbar)"/>
            </Grid>
            <!-- grid for user titles-->
            <Grid Margin="10, 2">
                
            </Grid>
        </Grid>

        <Grid Grid.Row="2" Background="#70C6E1">
                <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
                <!-- booklist -->
                <ListView Grid.Column="0" Margin="10" x:Name="BookListView" ItemsSource="{Binding BookList}" SelectedItem="{Binding SelectedBook}">
                    <ListView.ItemTemplate>
                        <DataTemplate DataType="common:BookViewModel">
                            <StackPanel >
                            <Grid Background="HotPink" Width="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType={x:Type Grid}}}">
                                <Grid.ColumnDefinitions >
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="2*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Margin="10" HorizontalAlignment="Center" Style="{StaticResource BooklistBookStyle}" Text="{Binding Model.BasicInfos.Titel}"/>
                                <TextBlock Grid.Column="1" Margin="10" HorizontalAlignment="Center" Style="{StaticResource BooklistBookStyle}" Text="{Binding Model.BasicInfos.Author}"/>
                                <TextBlock Grid.Column="2" Margin="10" HorizontalAlignment="Center" Style="{StaticResource BooklistBookStyle}" Text="{Binding Model.ExemplarBindingForSuperAwsomeWPFLibraryView}"/>
                            </Grid>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <!-- userlist -->
                <ListView Grid.Column="1" Margin="10">

                </ListView>
        </Grid>

        <!-- Grün = funktionsfähig, Orange = vorhanden, aber geht nicht, Rot = nicht vorhanden -->
        <StackPanel Grid.Column="1" Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Center" >
            <Button Content="Add Person" Command="{Binding AddPersonCommand}" Margin="10" Background="LawnGreen"/>
            <Button Content="Edit Person" Command="{Binding EditUserCommand}" Margin="10" Background="LawnGreen"/>
            <Button Content="Remove Person" Command="{Binding RemoveUserCommand}" Margin="10" Background="Red"/>
            <Button Content="Add Book" Command="{Binding AddBookCommand}" Margin="10" Background="LawnGreen"/>
            <Button Content="Remove Book" Command="{Binding RemoveBookCommand}" Margin="10" Background="Orange"/>
            <Button Content="Edit Book" Command="{Binding EditBookCommand}" Margin="10" Background="Red"/>
        </StackPanel>
    </Grid>
</UserControl>
